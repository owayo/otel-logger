# AGENTS.md

このリポジトリで作業する agent 向けのメモです。

## 基本

- 回答は日本語で行う。
- Rust 2024 edition / MSRV 1.98 の CLI + library project として扱う。Rust の版は `mise.toml` が正で、Cargo.toml の `rust-version` と Dockerfile のベースイメージも同じ版にそろえる。
- 変更前に `astro-sight context --dir . --git`、変更後に `astro-sight impact --dir . --git` を実行する。
- code identifier の利用箇所を探す場合は `grep` / `rg` ではなく `astro-sight refs` を使う。
- `CLAUDE.md` は `AGENTS.md` への symlink なので直接編集しない。

## よく使う検証

開発コマンドは `make help` (引数なしの `make`) で一覧できる。Makefile は cargo を `mise exec` 経由で呼ぶので、`mise.toml` の版で動く。

```bash
make setup     # mise のツールチェーンと依存を取得する
make fmt       # 整形する (書き換える)
make ci        # CI と同じ検査 (fmt-check、clippy -D warnings、test)
make release   # リリース版をビルドする
cargo audit    # RustSec advisory database で Cargo.lock を検査する (cargo-audit は別に入れる)
```

`make install` は release build を一時ファイルへコピーし、`mv -f` で `/usr/local/bin/otel-logger` を置き換える (cp で上書きすると macOS で SIGKILL されるため。署名はリンカが付けた ad-hoc 署名のままでよい)。

## 実装上の注意

- 受信した OTLP payload は JSON Lines で欠落なく保存する方針を守る。JSONL 書き込みに失敗した場合は HTTP `503 Service Unavailable` / gRPC `Status::unavailable` を返し、OTLP exporter 側に retry させる (`sink::Sink::record` の戻り値経由)。**OTLP が retry を許すのは HTTP 429 / 502 / 503 / 504 と gRPC `Unavailable` 系だけで、それ以外の 4xx / 5xx と `Internal` は "MUST NOT be retried" (client は payload を drop する)**。「5xx なら再送される」という直感は誤りで、500 / `Internal` を返すと欠測に直結するため、retryable な code を選ぶこと (回帰テスト: `persistence_error_maps_to_retryable_status`、`persistence_status_maps_to_retryable_code`)。累計使用量は JSONL 永続化に成功した batch だけを集計し、retry 対象を二重計上しない。stdout への pretty 出力はベストエフォートで、失敗してもログだけ残して継続する。
- JSONL `write_line` は batch ごとに `BufWriter::flush` / `LogRoller::flush` を呼び、ACK 時点で kernel に書き渡しておく。`sync_all` まで毎回呼ぶと throughput が落ちるため、disk 確定は graceful shutdown 時の `flush()` (= file の場合 `sync_all`) に委ねる。これで小さい batch がメモリバッファに留まったまま process crash で消える事故を防ぐ。writer の構造を変える場合は、単体ファイル経路だけでなく `record_persists_payload_to_rotated_directory_sink` で `log-dir` 経路も必ず確認する。
- Claude の API request ログには token/cost が含まれ、metrics より新しい分まで即時に届くことがある。累計統計では同じ model/effort のログが見えたらログ側を token/cost source として採用し、先に計上した metrics 分は取り消して二重計上を避ける。
- Claude / Codex の累計統計は二重計上を避ける。特に Codex は SSE 完了ログと turn metrics の両方に token usage が出るため、model ごとに最初に観測した片方だけを token source として採用する。実ログでは SSE 先着と metric 先着の両方があり、metric 先着時も後着 SSE と token 種別ごとの合計が一致する。metric を採用済みの model では後着 SSE を加算しない (回帰テスト: `codex_metric_source_prevents_later_sse_double_count`)。
- 2026-09-06 のローカル実ログ (Codex 0.153.4) では、同じ `gpt-5.6-sol/xhigh` について、先行 conversation の SSE と対応する turn metrics が揃った後にも、別 conversation の新しい SSE 完了ログが続いていた。最初に Logs を token source として採用した model では、途中の metric snapshot を無視した後も Logs の採用を維持し、新しい conversation の SSE usage を加算し続ける。metric 到着時に source を切り替えると、その時点より新しいログを欠落させる (回帰テスト: `codex_later_same_model_sse_is_counted_after_metric_snapshot`)。
- Codex の cache write token は、SSE `response.completed` の `cache_write_token_count` と metric `codex.turn.token_usage` の `token_type=cache_write_input` の両方を `ModelStats.cache_creation_tokens` に集計する。片方を token source として採用した model では、もう片方を二重計上しない (回帰テスト: `codex_sse_response_completed_log_counts_token_usage`、`codex_token_metric_skips_total_and_maps_others`)。
- Codex の SSE 完了ログで `input_token_count == tool_token_count` かつ output/cache-read/cache-write/reasoning がすべて 0 の tool-only event は、turn metrics / `handle_responses` span usage と合わせるため token usage に加算しない。cache write が 1 以上なら実 usage として必ず集計する (回帰テスト: `codex_sse_tool_only_shape_with_cache_write_is_counted`)。2026-08-28 に取得した CI 実ログ (Codex 0.150.1、2 conversation / SSE 27 件) では、**SSE 合計から tool-only 分を差し引いた値が `codex.turn.token_usage` metric と全 token 種別で完全一致した** (input 1,513,467 / output 17,618 / cached_input 1,316,096 / reasoning_output 9,550 / cache_write_input 0)。tool-only 判定を緩めたり外したりすると、この一致が崩れて metric 先着時と SSE 先着時で累計がずれる。実ログの `tool_token_count` は「input + output」相当の値になるため、`tool_token_count` 単体を total とみなす実装にも変えない (回帰テスト: `codex_mixed_tool_only_and_real_sse_totals_exclude_tool_only`)。
- 2026-09-26 の成功した CI `review` ジョブ (Codex 0.157.0) でも、tool-only 2 件を除いた SSE と turn metrics の合計が全 token 種別で一致した (input 4,855,252 / output 24,436 / cached_input 4,452,864 / reasoning_output 11,957 / cache_write_input 0)。ローカルの Codex 0.157.0/0.157.1 では `gpt-6-astra` / `gpt-6-sol` の SSE に `model_reasoning_effort` が付き、既存の conversation 単位の分類で扱える。進行中のローカルログでは SSE と metrics の到着範囲が異なるため、単純な全量比較を回帰判定に使わない。
- Claude Code の `claude_code.api_request` には `cost_usd` に加えて `cost_usd_micros` が入る (2026-08 時点の実ログで併存)。累計は引き続き `cost_usd` (double) を採用する。同ログには `speed` (fast mode の有無) や `skill.name` なども増えているが、バケットは provider/model/effort のみで切る。未知の属性が増えても既存の集計は壊れない。2026-09-06 の Claude Code 2.1.261 実ログでも API request ログと token/cost metrics のモデル別合計が一致した (回帰テスト: `claude_api_request_log_reads_double_cost_and_ignores_extra_attrs`)。
- 2026-09-26 の CI 実ログ (Claude Code 2.1.282) でも `claude-opus-5-5/high` の API request ログと metrics は input/output/cache creation/cache read/cost の全種別で一致した。ローカルの 2.1.282/2.1.283 では同モデルの API request ログが `claude-opus-5-5`、metrics が `claude-opus-5-5[1m]` を送る。実在する bare 版と混同しないよう、既存の canonical model 判定を維持する。
- Claude Code は `skill_activated` / `at_mention` / `retention_sweep` / `feedback_survey` / `plugin_loaded` / `hook_registered` / `mcp_server_connection` など token/cost を持たないログイベントも送る。`extract_claude_api_request_meta` は body が `claude_code.api_request` と完全一致する record だけを usage として扱うため、これらは累計へ混入しない。新しいイベント名が増えても分岐を足す必要はない (回帰テスト: `claude_non_api_request_log_events_do_not_affect_usage`)。
- Codex 0.140.0 以降の `codex.websocket_event` の `response.completed` は、実ログ上 token usage を持たず、対応する usage は `codex.sse_event` / `codex.turn.token_usage` / `session_task.turn` / `session_task.review` span 側に出る。usage を持たない websocket completion で token source を Logs に切り替えない。`session_task.turn` / `session_task.review` span の `codex.turn.token_usage.*` は metric / SSE と同じ usage の別表現なので、trace span からは二重計上しない。
- Codex の trace span event は、tracing exporter によって event の `name` が source location (`event otel/src/events/session_telemetry.rs:...`) になり、論理名 `codex.conversation_starts` が `event.name` 属性へ格納される。`otel_event_matches` で現行形式と論理名を直接持つ旧形式の両方を判定し、provider/effort metadata を欠落させない (回帰テスト: `codex_conversation_start_event_accepts_legacy_name_and_attribute_name`)。log record は body / `event.name` 属性 / top-level `LogRecord.event_name` の3形式を受け付ける (`codex_conversation_start_top_level_event_name_registers_session`)。2026-09-10/11 のローカル実ログ (Codex 0.153.4 / 0.154.0) では、**log record 側も top-level `event_name` が source location になり、論理名は `event.name` 属性にしか入っていなかった**。top-level だけを見て論理名を決める実装にすると `codex.sse_event` を含む全イベントを取り逃がすため、`event.name` 属性を必ず優先して解決する。
- Codex の SSE 完了ログは `conversation.id` で `codex.conversation_starts` の provider/model/effort に紐付ける。複数 conversation が混在するため、単純な「直近 session」だけで effort を決めない。`conversation.id` が付与されているが対応する session が未受信の場合は、別 conversation の session にフォールバックしない。SSE に `model_reasoning_effort` があればその値を保持し、無ければ `effort=unknown` のまま `codex_pending_sse` に保留する。後から session が届いた時は `merge_codex_pending_sse` で当該 conversation だけを確定バケットへ統合する。
- `update_codex_effort_from_request_attrs` (`handle_responses` span 経由の effort 補完) も `conversation.id` 単位で動かす。span に `conversation.id` がある場合は対応する `codex_sessions` の effort だけを更新し、同じ service の別 conversation の last session を巻き込まない。`conversation.id` が無い古い span のみ、同じ service の最後の session に対する fallback として動作させる。
- `conversation.id` / effort を持たない Codex turn metrics と旧形式の SSE / span は、`service.name` ごとの `codex_last_sessions` へだけフォールバックする。TUI (`codex_cli_rs`) と Exec (`codex_exec`) などが同時稼働しても、別 service の provider/model/effort を引き継がせない。実ログでは TUI の `gpt-5.6-sol/xhigh` metrics が、後着した Exec の `gpt-5.4-mini/low` session に誤分類される順序を確認している (回帰テスト: `codex_metrics_use_last_session_from_same_service`)。
- `merge_codex_pending_sse` は `conversation.id` 単位で pending 分だけを移動する。並行する別 conversation の token を巻き込まないよう、`codex_pending_sse` に conversation 別の累計を保留し、対応する session が届いた conversation の分だけ暫定バケットから確定バケットへ振り替える。conversation.id が無い古い telemetry に限り、unknown バケット全体を移す旧挙動を残す (ただし他 conversation の pending 合計は差し引いて取り違えを防ぐ)。pending エントリの lookup は `(model, conversation_id)` で行う。SSE 完了ログが session 未着のまま届くと、SSE 時点では session の provider が未知のため pending key は `codex_provider_from_session(None)` のデフォルト (OpenAI) で keyed される。後で `conversation_starts` が非デフォルト provider (Azure や openai-compatible custom endpoint) で到着した場合、`provider=session の provider` だけで pending を引くと miss してしまうため、conversation_id でマッチする pending すべてを取得する。各 pending エントリは SSE の effort が既知ならその値を保持し、unknown の場合だけ session の effort で補完したうえで、SSE 時の provider バケットから session の provider バケットへ個別に移す (回帰テスト: `codex_late_session_with_nondefault_provider_moves_pending`)。
- `log-file` / `log-dir` の相互排他は優先順位 (CLI/環境変数 > 設定ファイル > 既定値) を守る。上位で片方を指定した場合、下位のもう片方は衝突扱いにしない。
- `--config`、`otel-logger init --path`、`log-file`、`log-dir` は先頭の `~` / `~/` を `$HOME` に展開する。shell が展開しない環境変数・設定ファイル由来の値も同じ扱いにする。`HOME` が空文字 (`HOME=""`、cron / systemd / コンテナで実在する) の場合は未設定と同等に扱い、`~/x` を CWD 相対パスへ化けさせない (`default_config_path` の空 `HOME` 弾きと挙動を揃える)。`~` 展開は `path.to_str()` ではなく `OsStr` のまま component 単位で判定する。`to_str()` 依存だと非 UTF-8 バイトを含むパス (Unix) で `None` になり展開が無音で抜け、CWD 直下に `~` ディレクトリを作ってしまうため (回帰テスト: `expand_user_path_expands_non_utf8_paths`)。
- `log-dir` cleanup は `otel-logger.YYYY-MM-DD` 形式かつ実在する暦日の日次ローテーションファイルだけを削除する。桁が合っていても `otel-logger.2026-99-99` のように成立しない日付は LogRoller の出力ではないため削除しない。`otel-logger.pid`、`otel-logger.stderr.log`、単体の `otel-logger.jsonl` など、同じ prefix の別用途ファイルも削除しない。`DirEntry::file_type().is_file()` で判定し、日付形式の symlink を追って削除しない。`--log-keep-days 0` のように 0 が渡ってきた場合は cleanup と log roller の両方で `max(1)` を適用して最低 1 日分は残す。cleanup 側だけ 0 のまま走ると `cutoff = now()` となり、起動直後の有効ファイルまで全削除してしまう (回帰テスト: `open_rotated_sync_with_zero_keep_days_retains_recent_files`、`rotated_log_filename_requires_a_real_calendar_date`、`cleanup_does_not_remove_symlink_named_like_rotated_log`)。
- **logroller の `max_keep_files` は使わない**。あちらの prune は `otel-logger.\d{4}-\d{2}-\d{2}` に一致するファイル名を**辞書順に並べて古い方から**消すだけで、日付が実在するかを検証しない。しかも compression 無効時は active file も削除候補に数える (crate 側のコメントが「compression 有効時は active file を数えてはいけない」と書いている裏返し)。そのため `otel-logger.2026-99-99` のような不正日付や未来日付のファイルが 1 つあるだけで、辞書順で手前にくる**書き込み中のファイル**が unlink される。Unix では open 済み fd への書き込みと ACK は成功し続けるため、エラーが出ないままプロセス終了時に全データが消える。保持は mtime と実在暦日を検証する `cleanup_old_rotated_logs` に一本化し、`RotatedWriter::cleanup_if_day_changed` が日付の変わり目に呼び直す (常駐したままでも保持日数が効くようにするため)。
- cleanup の失敗は握り潰さない。`entry.metadata()` / `remove_file` の失敗を捨てると、保持期間が実際には効いていなくても起動が成功し、ディスクを使い切った時点で JSONL 永続化そのものが止まる。並行削除による `NotFound` だけを正常系として扱い、それ以外は呼び出し元へ伝播する。
- `otel-logger init` の上書き拒否は `OpenOptions::create_new` で atomic に行い、`exists()` 後の TOCTOU race で `--force` 未指定の既存ファイルを壊さない。
- 既定設定ファイルの存在確認は `Path::exists()` ではなく `try_exists()` を使う。`exists()` は権限エラーや symlink loop などあらゆる `metadata()` エラーを `false` に潰すため、設定が実在するのに読めない場合でも組み込み既定値で起動してしまう。その際 `log-file` / `log-dir` 設定ごと失われ、「JSONL 永続化なしで OTLP を受理して 200 を返す」= 無音で欠測する状態になる。
- TOML のパースエラーは `Error::set_input(None)` で元テキストの添付を外してから返す。toml crate は既定でエラー箇所の行をそのまま描画するため、設定に書いた `Authorization = "Bearer ..."` のような秘密が、構文エラーひとつで stderr / journal に平文で残る。`env:VAR` で秘密を隠す設計と矛盾させない。
- pretty stdout 出力 (`format::quote_for_pretty`) では ANSI escape を含む C0/C1 制御文字を必ず escape する。body/属性値だけでなく service 名、scope 名、span 名、metric 名、severity text、属性キー、累計サマリー内の provider/model/effort も対象にする。OTLP は既定で `0.0.0.0` に bind するため、信頼できない telemetry source からの terminal escape injection を防ぐ。JSONL 出力は lossless のまま raw 値を保持する (回帰テスト: `render_logs_escapes_untrusted_labels_and_attribute_keys`、`render_traces_escapes_untrusted_labels`、`render_metrics_escapes_untrusted_labels`、`render_summary_escapes_untrusted_bucket_metadata`)。Unicode の双方向制御文字 (`U+202A..U+202E` / `U+2066..U+2069` / `U+200E` / `U+200F` / `U+061C`) も同じ扱いで escape する。これらは表示順だけを入れ替えるため、`report\u{202e}txt.exe` を無害な名前に見せかけられる (回帰テスト: `quote_for_pretty_escapes_bidi_control_characters`)。
- proxy が上流から受けた非 2xx の**レスポンス本文はログに出さない**。`reqwest::Response::text()` は本文全体をメモリへ展開する (gzip なら展開後のサイズ) ため、読んでから `take(N)` しても上限にならず、内容も上流が自由に決められるので terminal escape injection の経路になる。診断は status code と route 名で足りる。`partial_success.error_message` を出す箇所も `Display` (`%`) ではなく `Debug` (`?`) を使い、制御文字を escape する。
- `/stats` の `agents.*.buckets` key は `provider/model/effort` 形式だが、provider/model/effort は外部 telemetry 由来なので `/` と `%` を `Bucket::key` で必ず percent-encode する。区切り文字と component 内の slash が衝突すると、別 bucket の累計が同じ JSON key に合流して壊れるため (回帰テスト: `bucket_key_escapes_separator_components`)。
- provider/model/effort を内部の複合キーに使う場合も、区切り文字で連結した文字列ではなく `ClaudeUsageKey` のような構造化キーを使う。percent-encode 済みの `Bucket::key` を split / replace して component を復元せず、`BucketStats` の metadata から `Bucket::from_parts` で移動先を作る (回帰テスト: `claude_usage_source_key_keeps_model_and_effort_separate`、`claude_log_then_metric_folds_separator_model_into_full`)。
- コメントは、外部に出る CLI help では英日併記、内部実装の説明では日本語を基本にする。
- `int_attr` / `f64_attr` / `number_value_as_u64` / `histogram_sum_as_u64` は `DoubleValue` / histogram `sum` を受け取る際に必ず有限性と範囲を検査する。OTLP は既定で `0.0.0.0` に bind され、信頼できない source から NaN / Infinity / range 外の double が届く可能性があるため、サチった `i64::MAX` / `u64::MAX` や `cost_usd=inf` で累計が破壊されないよう、有限かつ範囲内の値だけを採用する。
- `ModelStats` の累計加算は必ず saturating にする。個々の値を検証しても、複数 batch の合算で `u64` overflow や `cost_usd=inf` が起こると `/stats` と summary が壊れるため、外部入力由来の counter は wrap / panic させない。
- OTLP/gRPC・OTLP/HTTP の 1 リクエスト上限は `OTLP_MAX_REQUEST_BYTES` (32MiB) に引き上げる。tonic 既定 4MiB / axum 既定 2MiB のままだと大きな batch が `RESOURCE_EXHAUSTED` / `413 Payload Too Large` で恒久的に拒否され、exporter が retry しても同じ size のため回復できず「受信した payload を欠落なく保存」方針が崩れる。`serve_grpc` では生成した各 service server に `max_decoding_message_size`、`http::router` には `DefaultBodyLimit::max` を設定する。実測の最大 batch (約 0.6MiB) に十分な余裕を取りつつ、`0.0.0.0` 公開 bind 時のメモリ枯渇を避けるため 32MiB を上限とする。
- OTLP/HTTP の `Content-Type` 判定 (`http::detect_encoding`) は media type (type/subtype) を大小文字非区別で比較する (RFC 9110)。`Application/X-Protobuf; charset=utf-8` のような大文字混じり・パラメータ付きの表記でも `application/x-protobuf` / `application/json` として受け付け、decode 前に `415 Unsupported Media Type` で正当な OTLP request を弾かない。`;` 以降のパラメータを除いた primary type を `eq_ignore_ascii_case` で判定する。
- `update_codex_effort_from_request_attrs` (`handle_responses` span 経由の effort 補完) は、`conversation.id` を持つ span では当該 conversation の `codex_sessions` だけを更新し、同じ service の別 conversation の last session を巻き込まない (`update_codex_session_with_last(.., false)`)。last session を上書きすると、`conversation.id` を持たない後続 metric (`codex.conversation.turn.count` / `codex.turn.e2e_duration_ms` など) の effort バケットが、無関係な別 conversation の span に引きずられる。`conversation.id` が無い古い span、または同じ service の last session と同一 conversation の場合だけ last を更新する。**例外: 当該 service の last session が未登録の初回 seed では、`conversation.id` 付き span でも last を設定する。上書き対象の別 conversation が存在せず、seed しないと後続 metric が `effort=unknown` バケットへ落ちて span から拾えた effort 情報が失われるためである (回帰テスト: `codex_handle_responses_span_with_conversation_id_seeds_empty_last_session`)。
- Codex 系の `service.name` 判定は `is_codex_service` ヘルパーへ集約する。現状の対象は TUI (`codex_cli_rs`)、Exec (`codex_exec`)、Apps Server (`codex-app-server`)、MCP Server (`codex_mcp_server`) の 4 種。MCP Server は Codex 0.146.1 / 0.147.0 の実ログで確認され、`codex.process.start` や `codex.sqlite.init.*` の運用 metrics を送る。Codex 0.140.0 以降の Apps Server (`codex_apps` 経由) は `codex.remote_models.*` のような運用 metrics を送る場合はあるが、token usage の `codex.turn.*` metrics は送らず、usage は logs (`codex.conversation_starts` / `codex.sse_event`) と一部 trace span 側に出る。これらを集計対象から外すと該当プロセス経由の telemetry が欠落する。新しい Codex バイナリが増えた場合は `aggregator::is_codex_service` と OpenAI proxy の組み込み service 名へ同時に追加する。
- Codex 0.144.1 以降の SSE `response.completed` に含まれる `model_reasoning_effort` は、対応する session の `reasoning_effort` より response に近い情報として優先する。2026-07-10 の CI 実ログでは `gpt-5.6-terra` の high/xhigh 全件で両者が一致した。session 未着でも high/xhigh を保持する一方、provider は暫定の OpenAI になるため、pending key は `(provider, model, effort, conversation_id)` を保持する。遅れて非デフォルト provider の session が届いたら、session 側の effort が空でも既知の SSE effort は変更せず provider だけを確定する。SSE effort が unknown の場合だけ session の effort で補完して、当該 conversation 分を確定バケットへ移す (回帰テスト: `codex_late_session_without_effort_moves_known_sse_effort_to_provider`)。
- provider/model/effort は固定 allowlist で制限しない。`gpt-5.6-terra` / `gpt-6-astra` や Fable のような新しい model 名も telemetry の文字列を lossless にバケットへ保持する。新モデル対応で分岐を追加するのは、model 名ではなく telemetry schema 自体が変わった場合に限る。
- Codex の `codex.turn.token_usage` は値 0 の data point (実ログの `cache_write_input=0` や、`sum` を省略した histogram) でも届く。0 の点で token source を Metrics に固定すると、以降その model の SSE 完了ログを恒久的に取り込めなくなるため、`value == 0` は計上も latch もしない。`codex.conversation.turn.count` / `codex.turn.e2e_duration_ms` も 0 で空バケットを作らない (回帰テスト: `codex_zero_value_token_metric_does_not_latch_source`)。Claude 側の `has_usage()` ガードと挙動を揃えること。
- `handle_responses` span が `codex.conversation_starts` より先に届いた場合、span から合成する session の provider は暫定値 (OpenAI) にすぎない。`CodexSession::provider_confirmed` で確定/暫定を区別し、暫定のうちは SSE usage を `codex_pending_sse` に積んだままにする。確定扱いにすると、後から Azure などの非既定 provider で session が届いても usage を移せない (回帰テスト: `codex_span_before_conversation_starts_moves_pending_to_real_provider`)。
- Claude の metrics が接尾辞なしの model 名 (`claude-opus-5`) を直接送ってきた場合、それは「ログで接尾辞が落ちた表記」ではなく 1M 無効版という実在する別バリアント。`claude_metric_models` に記録し、`register_canonical_claude_model` / `canonical_claude_model` の両方で完全名への昇格・畳み込みを抑止する。単価が違うため混ぜるとコスト按分がそのまま誤る (回帰テスト: `claude_bare_metric_model_is_not_folded_into_variant`)。
- `claude_usage_source_key` は model を bare 化するため、1 つの source key が `claude-opus-5` と `claude-opus-5[1m]` の 2 バケットに対応しうる。metrics 分の取り消しは `claude_metric_usage` にバケット単位 (`BTreeMap<Bucket, ModelStats>`) で保持し、計上した先へそのまま返す。合計をログ側の 1 バケットへ押し付けると、引ききれない分が bucket 側で 0 にクランプされ `total` だけ余分に減って `total == Σbuckets` が壊れる (回帰テスト: `claude_metric_usage_is_cancelled_per_bucket`)。
- OTLP/HTTP は `Content-Encoding: gzip` の request を受け付ける (`OTEL_EXPORTER_OTLP_COMPRESSION=gzip` の exporter 対応)。解凍しないと gzip のマジックバイトを protobuf として読んで 400 になり、400 は retry 禁止なので batch が恒久的に失われる。`RequestDecompressionLayer` は `DefaultBodyLimit` より外側に置き、size 上限が解凍後の body に効くようにする (仕様が要求する "including after decompression"。回帰テスト: `router_accepts_gzip_encoded_body`、`router_rejects_gzip_body_larger_than_the_otlp_limit_after_decompression`)。OTLP/gRPC 側も `accept_compressed(CompressionEncoding::Gzip)` で同じ対応をする。受け付けないと handler 到達前に `Unimplemented` (非 retryable) が返り、HTTP の 400 と同じく batch が恒久的に失われる。片方だけ対応すると transport によって欠測するかどうかが変わる。
- **pretty / summary の stdout 出力は OTLP の ACK 経路から切り離す**。`Sink::record` は bounded channel (`PRETTY_QUEUE_CAPACITY`) へ `try_send` するだけで、実際の書き込みは単一の `run_pretty_writer` task が行う。ACK 経路で `write(2)` を待つと、stdout の読み手が遅いだけで全 batch の応答が止まる (`| less` でスクロールを止める、launchd / systemd / Docker の log driver が詰まる等)。その時点で JSONL 永続化と累計集計は既に完了しているため、exporter が timeout して再送すると**同じ payload が JSONL に二重に書かれ、累計も二重計上される**。`StdoutState::broken_pipe` が救うのは EPIPE (読み手が消えた) だけで、「読み手は生きているが読まない」ケースは素通りしていた。queue が溢れた分は捨て、`pretty_dropped` を 2^n ごとに warn する (stdout はベストエフォート、JSONL 側に payload は残る)。
- `--summary` の累計は **writer task が書く直前に** `snapshot()` する。writer は 1 本しかないので snapshot 順 = 出力順になり、単調増加のはずの累計が出力上で逆行しない。**enqueue 時に snapshot してはいけない** — 複数の producer が snapshot と enqueue の間で入れ替わり、この契約が壊れる。writer を 1 本化したことで、`stdout_lock` のような粗い排他も不要になった (旧実装は `MutexGuard` を呼び出し元 future に置き、書き込みは `spawn_blocking` 内で行っていたため、handler が cancel されると「lock は解放されたのに書き込みは継続中」になり、EPIPE の latch も落ちていた)。
- graceful shutdown には猶予 (`SHUTDOWN_GRACE` = 10s) を設ける。axum / tonic は in-flight request の完了を無制限に待つため、`Content-Length` を宣言して body を送り切らない接続が 1 本でもあると SIGTERM で終了できず、末尾の `sink.flush()` (= `--log-file` 経路の `sync_all`) に到達しないまま SIGKILL される。既定 bind が `0.0.0.0` なので外部から誘発できる。猶予を超えたら listener task を abort して flush へ進む (abort した in-flight は ACK していないので exporter が retry でき、欠測しない)。**flush 自体には timeout を掛けない** — `spawn_blocking` 内の `sync_all` は future を abort しても syscall が止まらず、「成功したことにする」方が危険。加えて 2 回目のシグナルで in-flight を諦める経路を持つ。tokio の signal handler はプロセスグローバルに既定動作を置き換えるため、受け直さないと 2 回目の SIGTERM も無視され SIGKILL しか手段が無くなる。10s は launchd の既定停止猶予 (20s) に対して `sync_all` 用の余白を半分残す値。
- OTLP/gRPC には `max_concurrent_streams` と `concurrency_limit_per_connection` を明示する (`MAX_CONCURRENT_STREAMS` = 16)。hyper は既定で 200 streams/connection を送るが、**tonic は `None` を明示してこれを打ち消す**ため、設定しないと gRPC だけが真に無制限になる。1 リクエストの上限 (32MiB) だけでは総メモリが抑えられず、TCP 1 本で並列に投げるだけで枯渇する。`timeout()` と `load_shed()` は付けない — 前者は JSONL 保存後・ACK 前に発火すると exporter の retry で二重計上を招き、後者は `Overloaded` が `Status` へ変換されず接続レベルのエラーとして漏れる。
- **stdout はプロセス側でローテーションしない**。fd 1 の所有者は親プロセス (launchd / systemd / container runtime) で、otel-logger には保存先のパスも保持契約も分からないため。代わりに、人が読める出力が有効なまま stdout が端末を指していない構成を起動時に 1 度だけ stderr へ警告する (`Settings::warns_unrotated_stdout` → `main::warn_if_stdout_unrotated`)。実例として macOS + launchd (`StandardOutPath` に生ファイル、`no-stdout = false`) で 37 日 7.6GB まで膨らんだ。判定を「stdout が通常ファイルか」ではなく「端末でないか」にしているのは、Docker の log driver のように pipe 経由でも保持管理を持たない経路があるため。`| less` では偽陽性になるが、警告の内容 (stdout はローテーションされない) はその場合も正しく、害は stderr 1 行に留まる。`--dry-run` でも出す (CI の smoke test で常駐構成の取り違えに気づけるようにする。回帰テスト: `warns_unrotated_stdout_only_when_output_is_enabled_and_redirected`)。
- `otel-logger init --daemon` は常駐運用向けの設定を生成する (`no-stdout = true` + 日次ローテーションの `log-dir`)。stdout を止めた先で単一 `log-file` を既定にすると、今度は JSONL が無制限に伸びる経路へ誘導するため、daemon profile では `log-dir` + `log-keep-days` を有効にする。既定 profile の生成物は変更しない (引数なしで出力を見る既存の使い方を壊さないため)。テンプレートは共通部分と profile 別の断片に分けて `render_config_template` で組み立てる。**全文を profile ごとに 2 本持たない** (proxy の説明のような無関係な箇所まで同期が必要になる)。**生成時に `no-stdout = false` を文字列置換しない** (表記を変えた瞬間に無音で壊れる)。回帰テスト: `daemon_profile_template_disables_stdout_and_rotates_jsonl`、`both_profiles_share_the_common_template_tail`、`daemon_profile_config_does_not_warn_about_unrotated_stdout` (警告文が勧める設定で実際に警告が止まること)。
- stdout への書き込みが `BrokenPipe` を返したら、以後の pretty / summary 出力を止める (`StdoutState::broken_pipe`)。`otel-logger | head` や pager の終了後も batch ごとに書き込みを試すと、失敗のたびに stderr へ error を積み上げ、stdout で止めた肥大化が stderr で再発する。EPIPE は telemetry の欠落ではないので `Ok` を返し、JSONL 永続化・累計集計・proxy 転送は継続する。それ以外の I/O error は一時的な可能性があるため恒久停止させない (回帰テスト: `stdout_broken_pipe_disables_further_pretty_output`、`stdout_transient_error_keeps_pretty_output_enabled`)。
- OTLP/HTTP の `Content-Type` が存在するのに `HeaderValue::to_str()` で解釈できない場合は、ヘッダー欠落時の protobuf 既定値へフォールバックせず `415 Unsupported Media Type` を返す。不正な media type を protobuf decode error (`400`) に化けさせない。
- OTLP proxy 転送 (`forward` モジュール) は「JSONL 保存を邪魔しない」順序を必ず守る。`Sink::record` が JSONL への永続化 + 累計集計を先に完結させてから、`ProxyRouter::notify` を呼ぶ。notify は bounded channel への `try_send` で fire-and-forget、失敗 (queue overflow / channel closed) は drop counter だけ上げて JSONL パスを止めない。この順序を入れ替えると、proxy 側の低速 / 障害が JSONL 書き込みまで詰まらせ「欠測しない」原則を壊す。
- proxy の `queue_depth` は `Sender::max_capacity() - Sender::capacity()` から snapshot 時に算出する。送信後の atomic increment と受信後の decrement で手動追跡すると、worker が increment より先に受信できるため underflow する (`queue_depth_uses_channel_state_after_receive` で回帰を防止)。channel closed で転送できなかった payload も `dropped_total` に加算する (`drops_when_channel_is_closed`)。
- proxy 転送を有効化した場合は `--log-file` か `--log-dir` のどちらかを必ず要求する。JSONL 永続化を持たないと process crash 時に in-flight batch が失われる余地が残り、上流 collector 側から見て欠測が発生するため。`cli::resolve_proxy_settings` で JSONL 出力先未指定は startup で reject する (回帰テスト: `merge_proxy_requires_jsonl_outbox`)。
- proxy route の service.name マッピングは組み込み既定 (anthropic=`claude-code`、openai=`codex_cli_rs`/`codex_exec`/`codex-app-server`/`codex_mcp_server`) を持つが、複数 route が同じ service.name を主張したら **startup 時に error** で reject する。同じ service.name が複数 lane にマッチすると意図しない二重送信と順序不定になり、上流側で二重計上を招くため (回帰テスト: `merge_proxy_service_name_conflict_is_rejected`)。空の service 名も、`service.name` が無い resource と一致して誤転送するため reject する (`merge_proxy_rejects_empty_service_name`)。
- 組み込み vendor route の config に endpoint がある場合、CLI / 環境変数の transport と header はその endpoint を再指定せず上書きできるようにする。設定の優先順位は CLI / 環境変数 > config であり、`build_vendor_route` で CLI endpoint の有無だけを上書き判定に使わない (回帰テスト: `merge_proxy_cli_options_override_config_route_without_repeating_endpoint`)。
- `http-protobuf` の proxy endpoint は起動時に絶対 HTTP(S) URL として検証する。相対 URL や空 URL を受理すると worker 起動後の全送信が retry/drop されるため、scheme/host が無い値と HTTP(S) 以外の scheme は reject する。OTLP の signal 別パスを endpoint 末尾に追加する実装なので、query / fragment 付き URL も startup error にする (回帰テスト: `http_client_rejects_invalid_endpoint_at_startup`)。
- proxy header の値には `env:VAR_NAME` prefix でプロセス起動時の環境変数解決を許す。secret を CLI 引数 / config ファイル / process listing に平文で残さないため。**プロセス一覧に晒れる `--proxy-*-header 'Authorization=Bearer xxx'` の直接指定は非推奨** (validate は許容するが CI 実運用では env: 経由にする)。`Host` / `Content-Length` / `Transfer-Encoding` は proxy 側で書き換えるべき hop-by-hop / body 制御ヘッダーなので、config で上書きしようとしたら error で reject (`merge_proxy_reserved_header_is_rejected`)。改行を含む値は HTTP header injection 対策で必ず reject (`merge_proxy_rejects_control_char_in_header_value`)。header 名は `resolve_headers` で **lowercase に正規化**し、同じ名前の重複は error にする。HTTP/2 (gRPC) は lowercase 必須で HTTP/1.1 も大小文字を区別しないため、config の `authorization` と CLI の `Authorization` を別 key として残すと、`BTreeMap` のバイト順反復と client 側の case-insensitive な insert によって**後着の config 値が CLI 値を上書きし、`CLI/環境変数 > 設定ファイル` の優先順位が逆転する** (回帰テスト: `merge_proxy_cli_header_overrides_config_header_with_different_case`)。
- proxy の HTTP client は **redirect を追わない** (`redirect::Policy::none()`)。reqwest の既定は最大 10 回追随するが、cross-host で除去されるのは `Authorization` / `Cookie` 系だけで、`api-key` (Azure) のようなカスタム認証ヘッダは残ったまま別 origin へ送られる。307/308 では OTLP payload ごと再送される。OTLP endpoint は明示指定なので redirect を追う正当性がなく、3xx は通常の送信失敗として retry させる。
- proxy endpoint に userinfo (`https://user:pass@host`) を書けない。起動時に reject する。URL 全体をエラーや context に載せると、送信失敗のたびに資格情報が stderr へ残るため、診断には route 名と signal path だけを使う。query / fragment を拒否するエラーも endpoint 自体を印字しない (query に API キーを書かれていると、拒否メッセージ自身が漏洩経路になる)。
- proxy の queue 容量は `tokio::sync::Semaphore::MAX_PERMITS` を超えないことを起動時に検査する。`mpsc::channel` は超過で panic するため、設定ミスが異常終了ではなく設定エラーになるようにする。
- proxy worker は初回送信に失敗した後、指数バックオフ (200ms → 30s cap) で `retry_max` 回まで再試行する (`retry_max=0` でも初回送信は 1 回行う)。backoff 中と送信中 request の両方で `CancellationToken` を監視し、shutdown 時は即座に abort する (設定した request timeout や無駄な sleep で SIGTERM を待たせない。回帰テスト: `retry_max_counts_retries_after_initial_attempt`、`send_with_retry_cancels_in_flight_request`)。retry 上限を使い切っても JSONL には payload が残っているため、Phase B (JSONL byte-offset outbox + per-route checkpoint) が入るまでは worker 再起動時の catch-up 経路が無いことを明示的に受け入れている。
- `--log-dir` 併用時の cleanup は、Phase A では proxy checkpoint との連動を実装していない。将来 Phase B で JSONL を crash-safe な outbox として扱うようになったら、全 route の checkpoint を通過していない rotated file を **削除しない** ように cleanup 側に条件を足す必要がある。それまでは「rotation 済み日次ファイルが消える前に proxy が追いつく必要がある」ことを運用側で意識する。
- proxy worker は shutdown で `biased` select を抜けたあと、queue に残っている batch を **`DRAIN_GRACE` (5s) の範囲で送り切る** (`drain_remaining`)。猶予ゼロで捨てると、上流の応答より ingest が速いだけの通常運用でも再起動のたびに route あたり最大 queue 容量ぶんが上流へ届かない (実測: 正常応答する上流に対して 40 件中 0 件到達)。Phase B の JSONL catch-up が入るまでは worker 再起動後の回収経路が無いため、そのまま上流から見た欠測になる。drain 中は retry しない (1 件の backoff で残り全部を落とす方が損失が大きい)。送信側へ渡す `CancellationToken` は shutdown token ではなく drain 専用の期限 token にする — shutdown token を渡すと `send_with_retry` が即 abort して 1 件も送れない。期限を超えた分は `dropped_total` に計上する。計上しないと `sent + failed + dropped` が notify 総数と一致せず、「上流へ渡せなかった件数」を運用側から観測できない (回帰テスト: `shutdown_drains_queued_batches_before_stopping`)。
- proxy の `TrySendError::Closed` は `warn` で出す。`Full` (一時的な背圧) と違い `Closed` は worker が死んだことを意味し、以降その route の全 batch が無言で drop され続ける。notify は receiver を止める前に来なくなる設計なので、稼働中に `Closed` が出るのは異常時だけ。`ProxyHandle::join` も `JoinError` を握り潰さず error で記録する (worker の panic を残せる唯一の場所)。
- JSONL / 設定ファイル / ログディレクトリは他ユーザーから読めない権限で作る (`crate::path::create_private_dir` = `0o700`、`restrict_new_file_mode` = `0o600`)。JSONL には受信した telemetry がそのまま入り、`user.email` / `user.id` / `organization.id` が含まれる (`OTEL_LOG_USER_PROMPTS` 有効時はプロンプト本文も)。設定ファイルには proxy の認証ヘッダを書ける。既定 umask 022 では 0644 / 0755 で作られ、共有 CI runner や多人数ログインサーバでは同一ホストの全ユーザーが読める。logroller が作る日次ファイルの mode は指定できないため、ディレクトリ側を 0700 にして守る。既存ファイル / ディレクトリの mode は変更しない (意図して group 共有にしている構成を壊さない)。
- `--log-dir` 経路も graceful shutdown で fsync する (`RotatedWriter::sync_rotated_files`)。logroller の `LogRoller::flush` は内部の素の `std::fs::File` に対する flush で、crate 全体に `sync_all` / `sync_data` が 1 箇所も無い = 実質 no-op。`--log-file` 側だけ `sync_all` していると、`init --daemon` が勧める常駐構成 (`log-dir`) の方がマシンクラッシュに弱いという逆転になる。どのファイルが書き込み中かを logroller が公開しないため、保持対象の日次ファイルをまとめて fsync する (shutdown 時の 1 回だけなのでコストは無視できる)。roller の lock は保持したまま fsync する — 先に解放すると並行 batch の書き込みが fsync の後ろに挟まる (回帰テスト: `sync_rotated_files_skips_files_that_are_not_daily_logs`)。
- `--no-stdout` / `--summary` は `bool` ではなく `Option<bool>` + `BoolishValueParser` にする。`bool` のままだと clap の strict な value parser が使われ、`OTEL_LOGGER_NO_STDOUT=1` を書いた systemd / launchd / compose の常駐構成が `invalid value '1'` で起動できない (README は `=1` を案内している)。`Option<bool>` は「未指定」と「明示的な false」も区別でき、`cli.x.or(config.x).unwrap_or(false)` で環境変数の `false` が設定ファイルの `true` に勝つ。`||` で合成すると優先順位 (CLI / 環境変数 > 設定ファイル) が逆転する (回帰テスト: `merge_boolean_flags_let_explicit_false_override_config`)。
- 空文字の `OTEL_LOGGER_*` 環境変数は未設定として扱う (`main::clear_empty_otel_logger_env`)。clap は空文字でも「指定あり」とみなすため、docker-compose の `environment:` や systemd の `Environment=` に空値を並べると `a value is required for '--log-file <PATH>'` で常駐プロセスが上がらない。`OTEL_LOGGER_PROXY_*_HEADERS=` に至っては「header を指定した」と誤判定されて endpoint を要求する。`config.rs` の `XDG_CONFIG_HOME` / `HOME` や `path.rs` の `~` 展開が空文字を未設定として扱うのと揃える。`NO_COLOR` も同様に空文字は無視する (no-color.org の仕様が "present and not an empty string")。
- proxy の endpoint / header に関わる引数には `hide_env_values = true` を付ける。clap は既定で `--help` に環境変数の**現在値**を描画するため、`OTEL_LOGGER_PROXY_ANTHROPIC_HEADERS=Authorization=Bearer ...` を設定した環境で `--help` を叩くだけで token が stdout (= CI ログ、チケット添付、画面共有) に出る。`ps` や `/proc/PID/environ` と違って権限の壁が無い。
- proxy header のパースエラーに entry 本文を載せない (`merge_cli_headers` は位置だけを出す)。`value_delimiter = ','` はカンマ区切りで複数 header を渡す OTLP 慣習 (`OTEL_EXPORTER_OTLP_HEADERS`) に合わせたものだが、カンマを含む値が分割されると `=` を持たない断片が生まれ、エラーメッセージとして値の後半がそのまま stderr / journald / CI ログに残る (回帰テスト: `merge_cli_headers_error_does_not_leak_the_value`)。
- proxy endpoint の検証は `validate_proxy_endpoint` に集約し、**gRPC / HTTP protobuf の両方**から呼ぶ。`ProxyTransport::default()` は gRPC なので、HTTP 側にしか検証が無いと既定経路が無防備になる。userinfo は `http::Uri` の authority に残り、tonic の `AddOrigin` 経由で HTTP/2 の `:authority` に載る (RFC 9113 §8.3.1 違反)。scheme 無しの endpoint は起動に成功したうえで全送信が恒久的に失敗する (回帰テスト: `grpc_client_rejects_invalid_endpoint_at_startup`)。
- `std::sync::Mutex` / `RwLock` の poison から回復する (`sink::lock_recovering`、`aggregator` の `unwrap_or_else(|p| p.into_inner())`)。poison は「以前どこかで panic した」記録でしかなく、JSONL の中身も集計の大半も壊れていない。ここで panic を再生産すると `spawn_blocking` が `JoinError` を返して**以後すべての batch が恒久的に 503** になり、retryable なので exporter が無限に再送し続けて 1 件も保存されない。
- `ingest_traces` は Codex service が 1 つも無い batch では write lock を取らずに返す。Claude Code は 2.1.278 以降 `claude_code.llm_request` / `claude_code.tool` / `claude_code.tool.execution` / `claude_code.interaction` / `claude_code.tool.blocked_on_user` の span を大量に送るが、いずれも集計対象外。これらで毎 batch 排他 lock を取ると、`/stats` の snapshot と並行 batch の ingest がその間ずっと待たされる。
- **Claude Code 2.1.278 の `claude_code.llm_request` span は token usage (`input_tokens` / `output_tokens` / `cache_read_tokens` / `cache_creation_tokens`) と `model` / `effort` を持つが、`claude_code.api_request` ログと 1:1 対応する同じ request の別表現**。実ログでログ 978 件と span 978 件が一致した。trace 側からも集計すると累計が倍になるため、`ingest_traces` が Codex service だけを見る契約を崩さない (回帰テスト: `claude_llm_request_span_is_not_counted_as_usage`)。
- `merge_codex_pending_sse` の pending マッチは **`conversation_id` だけ**で行う。pending キー自身が model を持つので session 側の model は不要で、`k.model == model` を要求すると `handle_responses` span から合成した session (model 属性を持たないことがある) で pending を解決できず、span から拾えた effort が失われて usage が `effort=unknown` バケットに取り残される。同じ理由で、`conversation_id` がある枝では `model.is_empty()` による早期 return をしない (`conversation_id` を持たない旧 telemetry 互換の枝だけ model を要求する)。
- `update_codex_session_with_last` は既知の値を空文字で上書きしない。`extract_session_from_attrs` は欠けた属性を空文字で埋めるため、`conversation.id` だけを持つ `conversation_starts` (resume や属性欠落) が成立する。そのまま insert すると span から回収済みの model / effort や確定済み provider を消してしまう。非空の新しい値は従来どおり勝たせる (回帰テスト: `codex_conversation_starts_without_attrs_keeps_span_model_and_effort`)。
- `merge_codex_pending_sse` は provider が暫定のうち (`provider_confirmed == false`) は pending を捨てず、確定した effort と暫定 provider で置き直す。捨てると「SSE → `handle_responses` span → `conversation_starts`」の順で届いたときに、後から本物の非既定 provider が来ても usage を移せず暫定バケットに取り残される (回帰テスト: `codex_span_after_sse_before_conversation_starts_moves_pending_to_real_provider`)。
- `subtract_from` で全項目 0 になったバケットは削除する。値を持たない provider/model/effort が `/stats` と summary に並ぶのを避ける (`move_codex_bucket_stats` と揃える。回帰テスト: `claude_metric_cancellation_removes_emptied_bucket`)。
- 2026-09-21 のローカル実ログ (Claude Code 2.1.278 / Codex 0.155.1、1,978 batch) と CI 実ログを OTLP/HTTP で再生し、`/stats` の累計がログ・metric の手計算と全項目で一致することを確認した。Claude: `api_request` ログ 987 件 (`claude-opus-5` / effort=max) が metric の `claude-opus-5[1m]` バケットへ正しく畳み込まれ、token 4 種と `cost_usd` (201.33979425) が一致。Codex: SSE `response.completed` 40 件から tool-only 5 件を除いた 35 件の合計が `codex.turn.token_usage` metric と全 token 種別で一致 (input 1,696,507 / output 47,521 / cached 1,484,928 / reasoning 22,786 / cache_write 0)。Codex 0.155.1 の新イベント (`codex.turn_ttft` / `codex.websocket_request` / `codex.websocket_connect` / `codex.startup_phase` / `codex.api_request` / `codex.tool_result` / `codex.tool_decision` / `codex.auth_recovery`) はいずれも token usage を持たず、`codex_non_usage_log_events_do_not_affect_usage` でカバー済み。
- **未対応として意図的に残している項目** — (1) aggregator の各 map (`buckets` / `codex_sessions` / `codex_pending_sse` / `claude_*`) にエントリ数上限が無く、`0.0.0.0` 公開時は外部 telemetry の cardinality だけメモリが伸びる。一律 LRU は不可: `claude_usage_sources` / `codex_token_sources` を evict すると「どちらを token source として採用したか」を忘れて logs と metrics を二重計上する。用途別に「累計バケット = 上限 + overflow 集約」「source map = admission freeze (上限超過後の未知 identity は集計せず JSONL のみ)」「session / pending = TTL + cap」と分けるのが正しい。(2) `/stats` が OTLP と同じポートに無認証で公開されている。別 listener (`127.0.0.1:4319`) への分離は README が「always on」と文書化している挙動を壊すため、opt-in 付きの破壊的変更として別途扱う。
